#include "main.h"
/**
 *  * _abs - Checks if a character is greater than 0.
 *   * @num: The character to be checked.
 *    *
 *     * Return: absolute value.
 *
 */
int _abs(int num)
{
if (num > 0)
{
return (num);
}
else
{
return (-num);
}
}
