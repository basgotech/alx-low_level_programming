#include "3-calc.h"

/**
 * op_add - a function that add a number
 * @a: store num1
 * @b: store num2
 * Return: sum of a+b
 */
int op_add(int a, int b)
{
return (a + b);
}

/**
 * op_sub - a function that substract a number
 * @a: store num1
 * @b: store num2
 * Return: substract of a+b
 */
int op_sub(int a, int b)
{
return (a - b);
}

/**
 * op_mul -a function that multiple a number
 * @a: store num1
 * @b: store num2
 * Return: multplied of a and b
 */
int op_mul(int a, int b)
{
return (a * b);
}

/**
 * op_div - a function that divide a number
 * @a: store num1
 * @b: store num2
 * Return: division of a and b
 */

int op_div(int a, int b)
{
return (a / b);
}

/**
 * op_mod - a function that operand a number
 * @a: store num1
 * @b: store num2
 * Return: operand of a and b
 */

int op_mod(int a, int b)
{
return (a % b);
}
